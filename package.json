{
  "name": "goreecloud-mail",
  "version": "0.1.0-dev",
  "private": true,
  "description": "Privacy-first GoreeCloud email client foundation",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "serve": "python3 -m http.server 8080 --directory web"
  },
  "engines": {
    "node": ">=22"
  }
}
